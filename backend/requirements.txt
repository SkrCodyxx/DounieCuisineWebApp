fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart

# Dépendances pour la base de données
SQLAlchemy>=2.0
psycopg2-binary
alembic>=1.9

# Dépendances pour la sécurité
passlib[bcrypt]
python-jose[cryptography]

# Dépendance pour le Rate Limiting
slowapi # Ajout de slowapi

# Outils de développement (à installer séparément ou dans le .venv globalement)
# black
# ruff
